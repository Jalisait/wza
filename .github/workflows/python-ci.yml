name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-lint:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout repository
      uses: actions/checkout@v3

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pylint

    - name: 🔍 Lint with flake8
      run: |
        flake8 main_module.py

    - name: 🔍 Lint with pylint
      run: |
        pylint --rcfile=.pylintrc main_module.py || true

    - name: 🧪 Run unit tests
      run: |
        python test_main_module.py
